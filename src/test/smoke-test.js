// import {expect} from 'chai';

describe('Core basic functionality', function () {
  it('lib should import without error', function () {
    require('../lib'); // eslint-disable-line global-require
  });
});
